# shared-ui-editor

This library was generated with [Nx](https://nx.dev).

It holds the rich-text editor component (`OkrEditor`, ngx-editor/ProseMirror-based) and its
toolbar configuration, split out of `@okr/shared-ui` so consumers that do not need the editor
do not pull ngx-editor/ProseMirror into their bundle.

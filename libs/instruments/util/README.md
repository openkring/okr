# instruments-util

Pure engine for the shared `instruments/ui` board primitive (spec §2.24): the declarative
`GridDefinition`, the canonical grid definitions (SWOT / PESTEL / BMC / Eisenhower), the
rank-ordered board utils, and the `InstrumentModel` factory/guard.

## Running unit tests

Run `nx test instruments-util` to execute the unit tests.

# instruments-feature

Smart layer for the instruments board (spec §2.24): `InstrumentStore`, the `InstrumentList`
(per-type list partition), the `InstrumentPage` (board detail rendering the shared
`okr-instrument-board`), and the instrument / topic edit modals.
